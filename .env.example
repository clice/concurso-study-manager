# Docker Compose reads a local .env file automatically.
# Copy this file to .env only if you want to override the development defaults.

DJANGO_SECRET_KEY=replace-this-only-if-you-want-a-custom-local-key
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
